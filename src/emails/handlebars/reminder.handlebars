<!DOCTYPE html>
<html>
<head>
    <style>
        .container {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .task-card {
            background-color: #f8f9fa;
            border-left: 4px solid #4CAF50;
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .time {
            color: #666;
            font-size: 14px;
            margin-bottom: 10px;
        }
        .task-name {
            font-size: 18px;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }
        .task-note {
            color: #555;
            line-height: 1.6;
        }
        .button {
            display: inline-block;
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 4px;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Task Reminder</h1>
        <div class="task-card">
            <div class="time">📅 {{formatDate time}}</div>
            <div class="task-name">{{taskName}}</div>
            {{#if taskNote}}
            <div class="task-note">{{taskNote}}</div>
            {{/if}}
        </div>
        <a href="{{appUrl}}/tasks/{{taskId}}" class="button">View Task in PlanPal</a>
    </div>
</body>
</html>